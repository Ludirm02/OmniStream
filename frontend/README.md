# OmniStream Frontend

Next.js interface for OmniStream AI.

## Run

```bash
cp .env.local.example .env.local
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL` in `.env.local` if backend is not on `http://localhost:8000`.
